/**
 * @author SaltedFish-No1
 * @description 登录页路由入口，静态导出为独立 HTML 页面。
 */

import { LoginView } from '@/features/auth/components/LoginView';

export default function LoginPage() {
  return <LoginView />;
}
